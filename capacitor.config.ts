import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.green.urbanly',
  appName: 'urbanlygreen',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
