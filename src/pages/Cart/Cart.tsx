import { IonContent, IonPage, IonLabel } from "@ionic/react";

const Cart: React.FC = () => {
  return (
    <IonPage>
      <IonContent fullscreen>
        <IonLabel>Cart Page</IonLabel>
      </IonContent>
    </IonPage>
  );
};

export default Cart;
