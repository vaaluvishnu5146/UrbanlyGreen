import { IonContent, IonPage, IonLabel } from "@ionic/react";

const Store: React.FC = () => {
  return (
    <IonPage>
      <IonContent fullscreen>
        <IonLabel>Store Page</IonLabel>
      </IonContent>
    </IonPage>
  );
};

export default Store;
