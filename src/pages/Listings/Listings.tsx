import { IonContent, IonPage, IonLabel } from "@ionic/react";

const Listings: React.FC = () => {
  return (
    <IonPage>
      <IonContent fullscreen>
        <IonLabel>Listings Page</IonLabel>
      </IonContent>
    </IonPage>
  );
};

export default Listings;
